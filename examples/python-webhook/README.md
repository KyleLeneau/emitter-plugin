# Python Webhook

Extremely minimal Http webhook for testing. Just run `uv run server.py` to start the server, then configure Emitter plugin to use `http://localhost:8000/webhook` as the URL (you might need to change the host for your network).
