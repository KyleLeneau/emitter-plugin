$schema: "http://json-schema.org/draft-07/schema#"
title: GuiderDitherData
type: object
description: Event after the guider has dithered
properties:
  stage:
    type: string
    enum:
      - After
required:
  - stage
